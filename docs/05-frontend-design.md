# 05. 프론트엔드 노선도 시각화 설계

전제: `04-api-design.md`의 API(특히 `/api/diagram`, `/api/stations/search`, `/api/stations/{id}`)를 소비하는 클라이언트를 설계한다. F1(노선도 시각화), F2(검색), F5(인터랙션), F6(범례)를 다룬다.

## 1. 기술 스택 (확정)

빌드 툴체인 없이 **순수 HTML/CSS/바닐라 JavaScript**로 진행한다. 내부 도구 규모에서 검색 자동완성, SVG 노선도, 상세 패널 정도의 상호작용은 프레임워크 없이도 충분히 구현 가능하고, 다음 이점이 크다.

- npm/Vite/React 등 빌드 파이프라인이 없어 **배포가 단순**함 — 정적 파일을 `src/main/resources/static`에 두기만 하면 Spring Boot가 그대로 서빙.
- 의존성이 거의 없어 유지보수 부담이 적고, 내부 도구 특성상 화면 복잡도가 과도하게 커질 가능성이 낮음.
- 브라우저 네이티브 ES 모듈(`<script type="module">`)로 파일을 기능 단위로 분리해 구조는 유지하면서 빌드 스텝은 생략한다.

- **렌더링 방식**: **SVG**. `document.createElementNS`로 노선/역 요소를 직접 생성. 역 수가 많아도(수천 개 이하 추정) DOM 성능에 큰 문제가 없고, CSS로 스타일링(hover, 하이라이트)이 쉽다.
- **상태 관리**: 별도 라이브러리 없이, 화면 상태(검색어/선택된 역/선택된 노선/하이라이트)를 담는 단순한 JS 객체 하나와, 상태 변경 시 관련 DOM을 다시 그리는 작은 렌더 함수들로 구성(간이 옵저버 패턴 정도로 충분).
- **파일 구조 (제안)**:

```
src/main/resources/static/
 ├─ index.html
 ├─ css/
 │   └─ style.css
 └─ js/
     ├─ api.js          // fetch 래퍼 (검색/역상세/노선/다이어그램 호출)
     ├─ state.js         // 화면 상태 + 구독/발행
     ├─ search.js        // 검색창, 자동완성
     ├─ diagram.js        // SVG 노선도 렌더링, 줌/팬
     ├─ detailPanel.js     // 역 상세 패널
     └─ main.js          // 초기화, 모듈 연결
```

- **배포 방식**: 별도 빌드 스텝 없이 정적 파일을 그대로 Spring Boot 안에 포함해 **애플리케이션 하나로 배포**한다.

## 2. 도식화(스키매틱) 레이아웃 전략

전체 107개 노선을 지하철 노선도 수준으로 한 번에 자동·정교하게 배치하는 것은 상당히 난도가 높은 과제(옥틸리니어 그래프 드로잉 문제)다. 현실적인 완성도를 위해 **3단계로 나눠 접근**할 것을 제안한다.

### 2.1 Stage A — 노선 단일 뷰 (우선 구현)

노선 하나를 선택했을 때, 그 노선에 속한 역들을 **가로 일직선(또는 완만한 꺾임)으로 균등 간격 배치**해서 보여주는 뷰. 좌표 계산이 단순해 알고리즘으로 자동 생성 가능(역 개수만큼 x축을 균등 분할, 환승 여부는 마커로만 표시). `LineStation.sequence_no` 순서만 있으면 백엔드 좌표 없이도 프론트엔드에서 즉시 계산 가능 — **`diagram_x/y`가 비어 있어도 동작하는 fallback 뷰**로 우선 구현한다.

### 2.2 Stage B — 지역(본부) 단위 통합 뷰

한 본부 관할 구역 내 여러 노선이 교차하는 모습을 보여주는 중간 규모 뷰. 노선 수가 상대적으로 적어(대부분 본부는 노선 10~20개 내외) 수작업으로 `diagram_x/y`를 배치할 수 있는 범위. `03-data-source.md`의 본부별 데이터 구축 순서와 맞물려, 데이터가 채워진 본부부터 이 뷰를 완성해간다.

### 2.3 Stage C — 전국 통합 뷰 (장기 목표)

모든 노선을 하나의 화면에 겹쳐 보여주는 최종 형태. 데이터/좌표가 충분히 쌓인 뒤 착수하며, 1차 배포 범위에는 필수로 포함하지 않는다(내부 도구이므로 검색 → 역 상세, 노선 선택 → Stage A/B 뷰만으로도 핵심 요구사항 F1~F4 충족 가능).

### 2.4 공통 레이아웃 규칙

- 노선은 가급적 **수평/수직/45도 대각선**만 사용해 꺾이도록 배치(지하철 노선도 관례).
- 역 간 간격은 실제 거리(km)에 비례시키지 않고 **가독성 우선으로 균등/유사 간격**을 기본으로 하되, 상세 정보(거리 km)는 클릭 시 텍스트로 정확히 제공.
- 환승역은 모든 소속 노선에서 동일한 좌표 한 점을 공유(02-data-model.md 2.5절과 동일 원칙).

## 3. 화면 구성 (DOM/모듈 단위)

컴포넌트 프레임워크가 없으므로 "컴포넌트"는 **HTML 영역 + 이를 담당하는 JS 모듈**로 대응한다.

```
index.html 레이아웃                담당 모듈
 ├─ #search-bar            (F2)  →  search.js
 ├─ #diagram-view           (F1/F5) →  diagram.js
 │   ├─ <svg> 안의 노선(line) 요소
 │   ├─ <svg> 안의 역(station) 요소
 │   └─ #legend             (F6)  →  diagram.js 내 범례 렌더 함수
 ├─ #line-list-sidebar        (F4)  →  main.js (노선 목록 fetch/렌더)
 └─ #station-detail-panel      (F3)  →  detailPanel.js
```

각 모듈은 `state.js`가 관리하는 상태(선택된 역/노선 등)를 구독해 자신이 담당하는 DOM만 다시 그린다.

## 4. 상호작용 흐름

| 동작 | 처리 |
|---|---|
| 검색창에 입력 | `/api/stations/search?q=` 디바운스 호출(예: 200ms) → 드롭다운에 후보 표시 |
| 검색 결과 선택 | `/api/stations/{id}` 호출 → `StationDetailPanel` 표시 + `DiagramView`에서 해당 역으로 팬 이동 및 하이라이트 |
| 노선도에서 역(점) 클릭 | 검색 결과 선택과 동일하게 상세 패널 표시 |
| 노선 목록에서 노선 선택 | 해당 노선 전체를 하이라이트, 나머지 노선은 dim 처리, Stage A(노선 단일 뷰) 또는 해당 노선이 지나는 Stage B 뷰로 전환 |
| 줌/팬 | 마우스 휠/드래그(데스크톱), 핀치/드래그(모바일) — SVG `viewBox` 조작 방식 사용, 별도 무거운 라이브러리 없이 자체 구현 가능한 범위 |

## 5. 반응형/모바일 대응

- 데스크톱: `StationDetailPanel`을 우측 고정 패널로 표시, `DiagramView`와 동시에 보임.
- 모바일(좁은 화면): `StationDetailPanel`을 하단 바텀시트로 전환, 노선도 위에 오버레이. `LineListSidebar`는 햄버거 메뉴/드로어로 축소.
- 브레이크포인트는 구현 시 일반적인 기준(예: 768px) 사용.

## 6. 데이터 없는 상태 처리

- `diagram_x/y`가 없는 역(Phase 2/4 진행 중 아직 좌표 미배치)은 Stage A 뷰(자동 균등 배치)로 항상 폴백 가능하므로, 데이터 구축이 완료되지 않은 노선도 검색·상세 조회 기능은 처음부터 100% 동작한다. 이는 이 프로젝트가 단계적으로 문서/데이터를 채워나가는 방식과 맞물려, **뷰가 데이터 완성도에 따라 점진적으로 고도화**되는 구조를 보장한다.

## 7. 다음 단계

`docs/06-search-ui.md`(검색/상세 UI 세부 설계)로 진행한다.

## 8. 공유 구간 표시 (2026-09-21)

전체 지도에서는 연속한 두 역의 ID 쌍을 방향과 무관하게 묶어 구간을 한 번만 그린다.
여러 노선이 같은 구간을 사용하면 회색, 단독 구간은 해당 노선 색으로 표시한다.
단순 교차나 가까운 좌표만으로는 서로 다른 선로를 합치지 않는다. 정차역 목록이 다른
운행계통의 건너뛰는 구간까지 동일 선로로 추론하지는 않는다.

왼쪽 목록에서 노선을 선택하거나 지도에서 단독 구간(다른 노선과 겹치지 않는 선)을 클릭하면
기본 연결망은 흐리게 하고, 원래 역 순서에 따른 전체 경로 하나를 그 위에 해당 노선 색으로
표시한다. 이 경로는 역 마커보다 아래에 놓인다. 다른 노선을 선택하면 같은 강조 요소의 경로를
교체하며, 같은 노선 재선택 또는 전체 보기로 해제한다. 공유 구간(여러 노선이 겹치는 선)을
클릭하면 지도에서 바로 강조하는 대신, 상세 패널에 그 구간을 지나는 노선 목록을 보여주고
그중 하나를 고르면 그때 강조·상세 조회가 이어진다 — 어떤 노선인지 특정할 수 없는 클릭을
임의로 하나로 확정하지 않기 위함이다. (역과 노선 선택을 서로 배타적으로 바꾼 이후의 최신
동작은 10장 참고.)

## 9. 지도 클릭 → 상세 패널 연동 (2026-09-21)

역을 클릭(지도/검색 결과/이전·다음역 링크)하면 `docs/06-search-ui.md` 3장의 역 상세를,
단독 노선 구간을 클릭(지도/왼쪽 목록)하면 그 노선의 개요와 전체 역 목록을 상세 패널에
띄운다. 두 종류의 상세는 상태(`state.js`)의 `detailView`(`{type, id}`) 하나로 관리해서
패널이 항상 "가장 최근에 고른 대상"을 보여주게 한다. 배경(빈 지도)을 클릭하면 역 상세는
닫히지만 노선 상세는 유지되는데, 노선 선택은 지도를 계속 탐색하는 동안 참고하는 용도라
쉽게 안 닫히게 둔 것이고, 역 상세는 지도를 둘러보다 실수로 켠 경우가 많아 배경 클릭 한 번에
닫히게 뒀다.

## 10. 클릭 히트 영역, 역/노선 선택 배타 처리, 상세 패널 디자인 (2026-09-21 추가)

- **클릭 판정 영역 확대**: 실제로 보이는 선(2px)·점(반경 4~7px 화면 픽셀)은 그대로 두고, 그
  위에 각각 `diagram-line-hit-area`(14px 폭), `diagram-station-hit`(반경 12~16px)라는
  눈에 안 보이는(`stroke`/`fill: transparent`) 훨씬 넓은 히트 영역을 겹쳐 그린다. 얇은
  선이나 작은 점 자체를 정확히 맞히지 않아도 그 주변을 클릭하면 인식된다. 실제 클릭 판정에
  쓰는 `.diagram-station-circle`/`.diagram-station-hit`처럼, 그리기용 클래스와 클릭 판정용
  클래스를 분리해뒀다 — CSS가 `.diagram-station-active circle`처럼 태그 셀렉터로 두 원 모두를
  잡아버리면 클릭 영역에도 강조 테두리가 그려지는 버그가 나므로 항상 클래스로 구분해서 건드린다.
- **역/노선 선택은 서로 배타적이다**: 역을 고르면(지도 클릭/검색/이전·다음역·노선 상세의 역
  목록 어디서든) 그 순간 `selectedLineId`도 함께 `null`로 지운다. 반대로 노선을 고르면(지도
  단독 구간 클릭/왼쪽 목록/공유 구간 선택 목록) `selectedStationId`도 함께 지운다. 그래서
  노선을 강조해 둔 채로 다른 역을 클릭해도 그 역 하나만 남고, 상세 패널을 닫으면(X 버튼)
  무엇을 보고 있었든 둘 다 지워져 전체 노선망으로 돌아간다 — 이전에는 타입별로 다르게
  지워서 잔상(예: 노선 상세를 보다가 역으로 넘어가도 이전 노선 강조가 안 풀리는 문제)이
  남았었다.
- **이전/다음역 디자인**: 역 상세의 각 노선 카드에서 이전역·다음역을 작은 밑줄 텍스트 링크
  대신 `station-nav` 3칸 레이아웃(이전역 버튼 ◀ / 현재역 pill / ▶ 다음역 버튼)으로 바꿨다.
  각 버튼은 `flex:1`이라 패널 폭에 맞춰 늘어나고, 역 이름은 굵게·구간거리는 작게 아래에
  둬서 눌러보지 않아도 다음 역이 뭔지 한눈에 들어오게 했다. 기점/종점은 버튼 대신 흐린
  "기점"/"종점" 표시로 구분한다.
- **노선 목록/검색창은 기본적으로 닫혀 있다**: `#line-list-sidebar`는 화면 크기와 무관하게
  기본이 닫힘 상태인 서랍(드로어)이고, 헤더의 햄버거 버튼(`#menu-toggle`)으로 여닫는다(모바일
  전용이 아니라 데스크톱에서도 동일). 검색창도 기본은 숨겨져 있고, 헤더의 검색 버튼
  (`#search-toggle`)을 누르면 열리며 상태(`searchOpen`)는 배경 클릭/Esc/역 선택 시 자동으로
  닫힌다. 노선 목록의 "폐선/중지 포함" 체크박스는 쓰임이 없어 제거했다 — 목록은 항상 정상
  운행 중인 노선만 보여준다(`GET /api/lines`를 `all` 파라미터 없이 호출).

## 11. 줌 범위·라벨 단계 조정 (2026-09-21 추가)

- `MIN_SCALE`을 0.4→1로 올렸다. scale=1이 이미 전체 노선망이 화면에 꽉 차는 배율(`baseScale`)
  이라, 그보다 더 축소하면 지도 밖 여백만 늘어날 뿐 의미가 없었다. 이제 휠로 축소해도
  "전체 보기" 지점 이상으로는 더 안 줄어든다.
- `MAX_SCALE`을 80→120으로 올려 한두 단계 더 확대할 수 있게 했다.
- 라벨을 단계별로 보여준다: 이전에는 하나의 임계값(`LABEL_SCALE_THRESHOLD`)을 넘으면 모든 역
  이름이 한꺼번에 나타나 조금만 확대해도 글자로 뒤덮였다. 이제 `LABEL_TRANSFER_THRESHOLD`(3)를
  넘으면 환승·분기역 이름만 먼저 보이고, `LABEL_ALL_THRESHOLD`(10)를 넘어야 모든 역 이름이
  나온다. `#diagram-content`에 `show-transfer-labels`/`show-all-labels` 두 클래스를 각각
  토글해서 CSS에서 둘을 구분한다.
- 라벨 글자 크기(`LABEL_FONT_PX`)는 줌과 무관하게 화면 픽셀 기준으로 고정돼 있는데(지도
  마커 라벨과 같은 방식), 기존 11px는 완전히 확대해도 작아 보였다. 실제 콘텐츠 좌표계 폭이
  4000×4000(`DiagramLayoutRunner.AREA_WIDTH/HEIGHT`)이라 `baseScale`이 약 0.23 정도로
  작게 나오고, 그 결과 `LABEL_FONT_PX`를 나누는 식이 실제 줌 범위 전체에서 상하한
  클램프(`MIN/MAX_CONTENT_FONT`)에 걸리지 않는 걸 확인했다 — 즉 13~14px 정도로 올려도
  다른 배율에서 부작용 없이 딱 그 크기로 고정 표시된다. 14px로 올렸다.
